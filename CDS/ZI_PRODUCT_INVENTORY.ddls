@AccessControl.authorizationCheck: #NOT_REQUIRED
@EndUserText.label: 'Product Inventory'

define root view entity ZI_PRODUCT_INVENTORY
  as select from zproduct_inv
{
  key product_id,
      product_name,
      quantity,
      price,

      quantity * price as stock_value
}
